# Assets

Runtime font binaries copied from installed apps are intentionally kept local
and ignored by git.

The composition CSS can reference local fonts when present and fall back to
system fonts when they are absent.

