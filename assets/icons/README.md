# Icons

Source-authentic icon assets live under `source-authentic/` by app family or
vendor. Prefer small, editable SVG sprites or inline SVG paths over CSS
pseudo-shapes when a real app icon is visible in a surface.

Keep large donor repos and generated icon packages out of this folder. Extract
only the small, reviewed symbols needed by a surface and document their source.
