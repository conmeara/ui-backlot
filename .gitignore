node_modules/
captures/
renders/
snapshots/
hyperframes-studio-preview.png
reference/claude/images/
reference/claude/videos/
reference/claude/frame-study/
reference/open-source/*/
# Dated ground-truth sets: manifests + token measurements are tracked; raw
# pixels of live/logged-in sessions stay local (personal content, asset policy)
reference/*/20*/**/screenshot*.png
reference/*/20*/**/screenshot*.jpg
# full element payloads contain page text from live sessions — local-only
reference/*/20*/**/elements.json
# Logged-in Playwright profile for live-app captures (cookies/session — never commit)
.playwright-profile/
# hyperframes capture output: downloads the target site's own assets/fonts
# (proprietary) — measurements may be distilled into tracked specs, raw
# captures stay local
reference/*/hf-capture*/
.hyperframes/
.playwright-mcp/
.thumbnails/
.DS_Store
.icon-donors/

# Loop working artifacts — one place, disposable, viewable via the review pages
workspace/*
!workspace/README.md
# Regenerated by every fidelity:score run
reports/fidelity/diff-latest.png
# Legacy scratch location (folded into workspace/scratch/)
scratchpad/

# motion ground truth: clips/frames are local-only (size); manifests tracked
reference/*/motion/**/clip*
reference/*/motion/**/frames/
